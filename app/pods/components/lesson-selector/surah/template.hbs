<article class='selector'>
  <HandleEscapeKey>
    <PaperSelect
      @dropdownClass='lesson-select-dropdown'
      @label="Surah Name"
      @placeholder="Surah Name"
      @required={{true}}
      @selected={{selectedSurah}}
      @options={{constants.SURAHS}}
      @searchField="transliteration"
      @searchPlaceholder="Search for surah.."
      @searchEnabled={{true}}
      @onChange={{action 'setSurah'}} as |surah|
    >
      {{surah.name_en}}
    </PaperSelect>
  </HandleEscapeKey>
</article>