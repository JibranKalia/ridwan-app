<PaperCard as |card|>
  <card.title as |title|>
    <title.text as |text|>
      <text.headline>{{lesson.formattedDate}}</text.headline>
    </title.text>
  </card.title>
  <card.actions>
    {{#if lesson}}
      <PaperButton @onClick={{action "deleteLesson" individualLesson}}>Delete</PaperButton>
      <PaperButton @onClick={{action "openModal"}}>Edit</PaperButton>
      {{!-- <LinkTo @class="base-link" @route="home.lessons.show" @model={{individualLesson.id}}>
        <PaperButton>Show Details</PaperButton>
      </LinkTo> --}}
    {{else}}
      <PaperButton @onClick={{action "openModal"}}>Create</PaperButton>
    {{/if}}
  </card.actions>
</PaperCard>

{{#if showModal}}
  <Lessons::Modal 
    @enrollment={{enrollment}}
    @type={{type}}
    @lesson={{lesson}}
    @lessonName={{lessonName}}
    @saveLessonItem={{action "saveLessonItem"}} 
    @closeModal={{action "closeCreateModal"}}
  />
{{/if}}